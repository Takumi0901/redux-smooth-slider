export const SLIDER = "SLIDER"

export function slider() {
  return {
    type: SLIDER
  }
}

export function sliderReducer(state={}, action) {
  switch(action.type){
    default:
      return state
  }
}